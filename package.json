{
  "name": "nextjs-start-template",
  "type": "module",
  "version": "0.0.0",
  "private": true,
  "packageManager": "pnpm@11.10.0",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "audit:ai-friendliness": "node scripts/ai-friendliness-audit.mjs static",
    "build": "turbo run build",
    "catalog": "pnpx codemod pnpm/catalog",
    "dev": "turbo run dev",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "postinstall": "lefthook install",
    "preinstall": "npx only-allow pnpm",
    "test": "turbo run test",
    "test:ai-friendliness": "node scripts/repo-tooling/ai-friendliness-gate/runner.ts",
    "test:ai-governance-tooling": "vitest run --testTimeout=120000 --maxWorkers=4 scripts/repo-tooling/ai-friendliness-audit scripts/repo-tooling/ai-friendliness-gate scripts/repo-tooling/ai-governance-e2e scripts/repo-tooling/architecture-policy scripts/repo-tooling/context-budget scripts/repo-tooling/workspace-validator",
    "test:repo": "vitest run --testTimeout=120000 --maxWorkers=4 scripts/repo-tooling",
    "test:run": "pnpm run test:repo && turbo run test:run",
    "typecheck": "pnpm run typecheck:repo && turbo run typecheck",
    "typecheck:repo": "tsc --noEmit -p scripts/repo-tooling/tsconfig.json",
    "up": "taze major -I -r",
    "verify": "pnpm run audit:ai-friendliness && pnpm run verify:lint && pnpm run verify:typecheck && pnpm run verify:versions && pnpm run verify:architecture && pnpm run verify:workspaces && pnpm run verify:fixtures",
    "verify:ai-governance:prepare": "node scripts/repo-tooling/ai-governance-e2e/runner.ts prepare",
    "verify:architecture": "node scripts/verify-conventions.mjs",
    "verify:fixtures": "vitest run scripts/repo-tooling/architecture-policy/runner.test.ts scripts/repo-tooling/workspace-validator/workspace-validator.test.ts",
    "verify:lint": "pnpm lint",
    "verify:typecheck": "pnpm typecheck",
    "verify:versions": "syncpack lint",
    "verify:workspaces": "node scripts/verify-workspaces.mjs"
  },
  "devDependencies": {
    "@antfu/eslint-config": "catalog:",
    "@commitlint/cli": "catalog:",
    "@commitlint/config-angular": "catalog:",
    "@commitlint/config-conventional": "catalog:",
    "@eslint-react/eslint-plugin": "catalog:",
    "@next/eslint-plugin-next": "catalog:",
    "@types/node": "catalog:",
    "eslint": "catalog:",
    "eslint-plugin-format": "catalog:",
    "eslint-plugin-react-refresh": "catalog:",
    "eslint-plugin-tailwindcss": "catalog:",
    "lefthook": "catalog:",
    "syncpack": "catalog:",
    "tailwindcss": "catalog:",
    "taze": "catalog:",
    "turbo": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
