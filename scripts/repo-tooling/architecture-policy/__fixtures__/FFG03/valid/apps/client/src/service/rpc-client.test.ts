import type { AppType } from 'api'
import { expect, it } from 'vitest'

export type ExpectedAppType = AppType

it('represents the colocated rpc client runtime test', () => {
  expect(true).toBe(true)
})
