# Large rule

This deliberately verbose fixture repeats durable governance guidance so the audit can report a document that exceeds its configured context budget without pretending that every long document is automatically invalid. The content is intentionally longer than the fixture threshold and remains a warning because prose quality requires review rather than a brittle mechanical verdict.
