# Next.js App Router

用于 App Router 页面、布局、Route Handler、Server Action、缓存与 Metadata 的任务导航。
当任务命中 `apps/*/src/app/**` 的路由或 runtime 行为时，通过 `/nextjs-app-router` 触发。
feature 业务实现与普通 React 组件不使用此入口。
入口与完整路由见 [SKILL.md](./SKILL.md)。
