# Single source of truth for create-package task routing.
# SKILL.md Common Tasks 表必须与本文件保持一致。

tasks:
  - intent: 新建共享包
    triggers: ["新建包", "新建 package", "提取共享包", "新模块抽包"]
    trigger_examples:
      - "帮我把这块逻辑抽成一个共享包"
      - "新建一个 package 放通用工具"
      - "这块代码要抽成独立模块"
    route:
      - workflows/new-package.md
      - references/package-anatomy.md

  - intent: 包结构与 exports
    triggers: ["exports", "package.json", "目录结构", "tsconfig"]
    trigger_examples:
      - "package.json 的 exports 字段怎么配"
      - "包的目录结构怎么组织"
      - "tsconfig 要怎么继承"
    route:
      - references/package-anatomy.md

  - intent: 依赖声明
    triggers: ["dependencies", "peerDependencies", "依赖边界", "workspace:*"]
    trigger_examples:
      - "这个依赖该放 dependencies 还是 peerDependencies"
      - "workspace:* 怎么用"
      - "包之间的依赖边界怎么定"
    route:
      - references/package-anatomy.md

  - intent: 踩坑
    triggers: ["幽灵依赖", "exports 解析失败", "references", "未声明依赖"]
    trigger_examples:
      - "提示幽灵依赖问题"
      - "exports 字段解析失败"
      - "用到了没声明的依赖"
    route:
      - references/gotchas.md
