# Single source of truth for project-architecture task routing.

tasks:
  - intent: 新代码放哪一层
    triggers: ["放哪里", "哪一层", "新模块", "组件放哪"]
    trigger_examples:
      - "这段新代码应该放在哪一层"
      - "新模块要建在哪里"
      - "这个组件该放哪个目录"
    route:
      - references/architecture-overview.md
      - references/directory-structure.md

  - intent: 跨层 import 合规性
    triggers: ["能不能 import", "依赖规则", "跨层"]
    trigger_examples:
      - "这层能不能 import 那层"
      - "跨层 import 有没有违规"
      - "项目依赖规则是什么"
    route:
      - references/architecture-overview.md
      - references/gotchas.md

  - intent: 目录结构
    triggers: ["目录", "文件组织", "structure"]
    trigger_examples:
      - "项目目录结构是什么样的"
      - "文件应该怎么组织"
      - "整体架构目录怎么分层"
    route: [references/directory-structure.md]

  - intent: 命名规范
    triggers: ["命名", "kebab", "PascalCase", "命名约定"]
    trigger_examples:
      - "文件命名用 kebab 还是 PascalCase"
      - "项目命名规范有哪些"
      - "组件名怎么命名"
    route: [references/naming-conventions.md]

  - intent: 踩坑
    triggers: ["循环依赖", "误用 @/app", "层级错乱", "跨包误引", "幽灵依赖"]
    trigger_examples:
      - "出现了循环依赖怎么解"
      - "不小心误用了 @/app 路径"
      - "层级关系错乱了"
    route: [references/gotchas.md]
