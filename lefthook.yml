commit-msg:
  commands:
    commitlint:
      run: npx commitlint --edit {1}

pre-commit:
  parallel: true
  commands:
    lint:
      glob: '*'
      run: pnpm lint:fix {staged_files}
    verify:
      glob: '*'
      run: pnpm verify
