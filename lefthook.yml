commit-msg:
  commands:
    commitlint:
      run: npx commitlint --edit {1}

pre-commit:
  parallel: true
  commands:
    lint:
      glob: '*.{js,jsx,ts,tsx}'
      run: pnpm lint:fix {staged_files}
    test:
      glob: '*.{ts,tsx}'
      run: pnpm vitest run --passWithNoTests --changed HEAD~1
