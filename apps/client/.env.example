# ===========================================
# Environment Variables Configuration
# ===========================================
# Copy this file to .env.local and fill in your values
# cp .env.example .env.local

# ===========================================
# Application Settings
# ===========================================

# Application URL (used for absolute URLs, callbacks, etc.)
# Required in production
NEXT_PUBLIC_APP_URL=/

# Application environment
# Options: development, staging, production
NODE_ENV=development

# ===========================================
# API Configuration
# ===========================================

# Base URL for API requests (server-side)
# Example: https://api.example.com
API_BASE_URL=

# Public API URL (accessible from client-side)
# Use NEXT_PUBLIC_ prefix for client-side access
# 指向 apps/api(Hono) 地址（如 http://localhost:3000），供 /demo/rpc 的 hc 扩展通道
#（hc<AppType> → apps/api）使用；不设则 hc 打 client 自身端口会 404。
# 注意：client 常规请求走自身 Route Handler（/app/api），不依赖此变量。
NEXT_PUBLIC_API_URL=

# ===========================================
# Authentication (Optional)
# ===========================================

# JWT Secret for token signing (server-side only)
# Generate with: openssl rand -base64 32
# JWT_SECRET=

# Session secret for cookie encryption
# SESSION_SECRET=

# ===========================================
# Database (Optional)
# ===========================================

# Database connection string
# Example: postgresql://user:password@localhost:5432/dbname
# DATABASE_URL=

# ===========================================
# Third-party Services (Optional)
# ===========================================

# Analytics
# NEXT_PUBLIC_GA_ID=

# Error tracking
# SENTRY_DSN=

# ===========================================
# Feature Flags (Optional)
# ===========================================

# Enable debug mode
# NEXT_PUBLIC_DEBUG=false

# Enable maintenance mode
# MAINTENANCE_MODE=false
