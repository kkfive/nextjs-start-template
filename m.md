OpenAI 内部有个项目，我每次讲上下文这个话题都会拿出来说，因为它太典型了。

他们用 Codex 从零搭了一个内部产品：百万行代码，零行人写，全部由 Agent 生成。三个工程师，五个月，1500 个 PR。这件事被他们写成了一篇文章，叫 Harness Engineering。

文章地址: https://openai.com/index/harness-engineering/

数字看起来很震撼，但这篇文章里真正值钱的部分是他们踩的一个大坑。

项目初期，他们做了一件几乎所有人都会做的事：写了一个巨大的 AGENTS.md，把所有规则、约束、架构原则、编码规范全塞进去。逻辑上这很合理——Agent 不是每次启动都失忆吗，那我把它需要知道的一切都写在开机必读文件里，总没错吧。

结果，用他们自己的原话说，"it failed in predictable ways"——以一种可以预见的方式失败了。

这一章我们就把这个问题拆开看，然后讲清楚正确的使用方式是什么。

先强调一个认知：这是你投入产出比最高的一个文件，没有之一
先把这个文件本身说清楚。

Agent 每次会话启动都是失忆的。你昨天跟它交代的技术栈、测试命令、那些绝对不能碰的目录，今天开新会话，它一概不知道。Claude Code 里面的CLAUDE.md（以及通用标准 AGENTS.md）是唯一能让它每次开工就"记住"你的东西——你可以把它理解成给 Agent 的入职文档。

这个文件的影响范围覆盖了你跟 Agent 的每一次交互。写好了，它从第一句话就知道该怎么干活；写砸了，每次对话都在重复纠正那些本不该出现的错误。上一章我们算过 review 的概率账，这里同样可以算一笔：假设一条缺失的规则让 Agent 每天多犯一次错、你多花五分钟纠正，一年就是三十个小时——而把这条规则写清楚只要一分钟。

顺便说一句，这个文件现在已经是行业级的标准了。2025 年 12 月，OpenAI 把 AGENTS.md 捐给了 Linux Foundation 旗下新成立的 Agentic AI Foundation，跟 MCP 放在同一个基金会里管理。Codex、Cursor、GitHub Copilot、Gemini CLI 这些工具全部原生支持，采用它的开源项目超过六万个。换句话说，你在这个文件上练出来的意识和功夫，换任何工具都不会白费。

巨型指令文件是怎么崩坏的
OpenAI 团队总结的失败原因，我用人话给你翻译一下，其实就两件事。

第一件事，注意力被稀释了。上下文窗口就那么大，你塞两百条规则进去，Agent 真正该关注的东西——当前任务、相关代码、需要参考的文档——反而被挤到了角落。更麻烦的是，两百条规则全都摆在"开机自启动"的位置，对 Agent 来说等于全部没有优先级。它分不清哪条是此刻必须遵守的、哪条是完全没用的。

这背后有一个硬性的模型限制。业界实测的经验是，前沿模型大概能可靠遵循 150 到 200 条指令，而 Claude Code 自身的系统提示已经占掉约 50 条，留给你的空间也就 100 到 150 条。超过之后发生的事情很反直觉：受影响的不只是新加的规则，是所有规则的遵循质量一起下降。

所以行数上给自己立个规矩：尽量 300 行以内，能更短就更短。

第二件事，上下文腐烂（Context Rot）。代码天天在变，架构在演进，但那个几百行的文件没人想去更新——写的时候人人都觉得"以后会维护的"，三周之后它就成了屎山文件。Agent 不知道哪些还有效、哪些早就过时了，人也搞不清楚。到这个阶段，这个文件从资产变成了负债：它还在每次会话里消耗上下文，但提供的信息已经开始出错。

你发现没有，这两个问题有一个共同的根源：试图在启动时把所有信息一次性灌给 Agent。信息越多，稀释越严重，腐烂越快。所以解法的方向也就清楚了——问题并不出在信息本身，出在信息出现的时机上。

百科全书式 AGENTS.md 与渐进式披露目录结构的对比

OpenAI 的解法：目录，不是百科全书
痛定思痛之后，OpenAI 团队做了一个关键转变：把 AGENTS.md 从"百科全书"改成了"目录"。

新的 AGENTS.md 只有大约 100 行。它不再试图把所有规则写进去，只干一件事：告诉 Agent "你需要知道的东西在哪里"。真正的知识放在一个结构化的 docs/ 目录里——设计文档、架构文档、产品规格、安全规范，每个都是独立文件，有清晰的分类和索引。

他们把这个模式叫 Progressive Disclosure（渐进式披露）：Agent 从一个小的、稳定的入口开始，在需要的时候才去读更深层的信息，而不是一开始就被所有东西淹没。

这个词听起来陌生，但你天天在用它。比如 iPhone 的设置页面从来不会把几百个开关一次性铺给你，它先给你十几个分类入口，你需要哪个再点进去一层。交互设计领域几十年前就想明白了这件事：人的注意力带宽有限，信息必须分层给。

现在轮到 Agent 了——它的"注意力带宽"就是上下文窗口，同样有限，同样需要分层。

真正的杠杆从来不在于你给 Agent 的信息量多大，而在于在对的时间，只给它需要的那一份。

而且这不是 OpenAI 一家的案例。你去看那些做得好的 Agent 系统，会发现它们在各自完全不同的场景下，收敛到了同一个模式：

Karpathy 做他的 LLM Wiki 知识库时，设计了一个三级结构：一个约 2500 字符的 index.md 常驻上下文，每个知识页面在里面只占一行摘要；Agent 需要细节时顺着索引去读对应页面；页面还不够，才回到最底层的原始文档。他的结论是，几百个页面的规模下这套机制"出奇地好用"，不需要向量数据库，一个索引文件加 grep 就够了。

Claude Code 的 Skills 系统也是同一个思路：启动时只加载每个 Skill 的名字和一句话描述（每个约 100 token），Agent 判断某个 Skill 跟当前任务相关了，才加载完整内容；Skill 目录下的参考文件和脚本，要到真正用的时候才去读。工具系统同理——低频工具只在上下文里留一个名字，需要时通过 ToolSearch 现场取回完整定义。

四个互相独立的系统——项目指令、个人知识库、技能加载、工具加载——不约而同走向同一个结构，殊途同归。放到你身上，我觉得培养出这样的意识是很有必要的：每次想往 Agent 的启动上下文里加东西时，先问一句，这个信息是每次会话都需要，还是只有特定任务需要？后者就不该放在默认的上下文里面，该放在目录里按需读取。

项目指令、个人知识库、技能和工具加载共同收敛到渐进式披露

落地：一个好的 CLAUDE.md 长什么样
原则讲完了，落到你自己的项目上，具体该写什么、放在哪。

先说放的位置。很多人不知道 CLAUDE.md 是一套层级系统，一共四个位置，作用范围各不相同：

~/.claude/CLAUDE.md 是全局的，你的个人偏好（比如"回复用中文"）放这里，所有项目都生效；
项目根目录的 ./CLAUDE.md 是主战场，团队共享，提交到 git；
子目录也可以放自己的 CLAUDE.md，monorepo 里前端目录一套规范、后端目录一套规范，各管各的，而且只在 Agent 碰到对应目录的文件时才加载——你看，层级系统本身就自带渐进式披露；最后是 CLAUDE.local.md，个人私有的项目配置，加进 .gitignore 不提交。四层可以叠加，从全局到具体依次读取。
再说组织内容的框架。HumanLayer 团队提过一个很好记的三维度：WHAT（项目是什么、技术栈、目录结构）、WHY（每个模块的职责、为什么选这个方案）、HOW（怎么跑测试、怎么构建、用什么包管理器）。围绕这三个维度写，基本不会漏关键的东西。

一个精简但信息量足够的骨架大概是这样：

markdown

复制

# 项目名：一句话说清楚这是什么

Next.js 14 项目，App Router + TypeScript。

## 常用命令

- `pnpm dev`：启动开发服务器
- `pnpm test src/xxx.test.ts`：跑单个测试文件，不要跑全量
- `pnpm typecheck`：改完代码必须跑一次

## 项目结构

- `/app` - 页面和路由
- `/lib` - 工具函数（跨模块共享的逻辑都在这，别重复造）
- `/prisma` - 数据库 schema，migration 文件不要手动改

## 重要约定

- 不要用 --force 推送，用 --force-with-lease
- 环境变量在 .env.local，不要提交
- 认证相关的改动，先读 @docs/auth-patterns.md
  这个骨架里藏着几条经验，值得单独说说。

命令要写到精确的语法。Agent 猜不到你用 pnpm 还是 npm，更猜不到你希望它跑单个测试文件。"注意测试性能"这种话等于没说，pnpm test src/xxx.test.ts 才是它能执行的指令。

只写它猜不到的。TypeScript 的基本语法不用教，README 里有的不用重复，linter 能管的不要写——"用 2 个空格缩进"这种规则交给 Prettier，确定性工具做确定性的事，又快又准，还不占你宝贵的指令预算。该写的是那些只存在于你脑子里的东西：为什么这个目录不能碰、当年踩过什么坑、哪个看起来冗余的逻辑其实是给老用户留的兼容——想想开篇第一章那个被"优化"掉的兼容逻辑，它就该活在这个文件里。

每个"不要"后面跟一个"应该"。光写"不要用 --force"，Agent 在需要强推的场景下会卡住或者自作主张；写成"不要用 --force，用 --force-with-lease"，它就有了明确的替代路径。

详细文档用引用，不要内嵌。CLAUDE.md 支持 @path/to/file 语法。但这里有个很多人踩的坑：直接 @ 引用会在每次启动时把整个文件嵌进上下文——你以为自己在做披露，实际上在做灌注。

这个坑我自己结结实实踩过一次。

我在 Sitor 里做了大量的用户行为打点，打点的说明文档写得很全——事件名、触发时机、参数含义，加起来好几万字。当时图省事，直接把它 @ 进了 CLAUDE.md。后果是每开一个 Claude 实例，接近 10K token 的打点文档就常驻在上下文里，不管这次任务碰不碰打点。

而实际上，我一天的任务里可能只有一两个跟打点有关。类似被我塞进去的长文档还有几份，加起来每个会话一启动就背着一大坨跟当前任务无关的东西。后来清理了一轮，@ 全部换成条件指引，整个会话立刻清爽了很多——这算是我在这个文件上买过最贵的一次教训。

正确的做法就是写成条件指引："涉及行为打点的改动，先读 docs/tracking.md"、"认证相关的改动，先读 docs/auth-patterns.md"。这样 Agent 只在碰对应模块的时候才去读那份文档。这就是渐进式披露在你自己项目里的最小实现。

规模再大一点，可以用 .claude/rules/ 目录做模块化：每个规则文件支持 YAML frontmatter 的 paths 字段，用 glob 匹配限定作用范围，比如认证模块的规则只在 Agent 碰 src/auth/** 下的文件时才生效。目录结构本身就成了披露的分层。

关键规则可以用强调语法加权。某条规则 Agent 老是不遵守的话，试试在前面加 IMPORTANT: 或者 YOU MUST:——Anthropic 官方文档明确说过这类关键词能提升模型对特定指令的遵循程度。但要克制，每条都加 IMPORTANT 就等于没加，这个道理跟前面说的"两百条规则全标高优先级"是同一个。

还可以指定压缩时保留什么。上下文快满的时候 Claude Code 会自动压缩对话历史，你可以在文件里写清楚压缩时必须保留的信息——比如"所有修改过的文件列表、测试命令和结果、当前的实现计划"。这样长会话被压缩后，关键状态不会跟着丢。

最后一个技巧是给多工具团队的。现在各家工具都有自己的配置文件名：Cursor 用 .cursor/rules，Copilot 用 .github/copilot-instructions.md，Gemini CLI 用 GEMINI.md。团队里工具不统一的话，别维护好几份重复内容——把规则全放进 AGENTS.md（它本来就是通用标准），然后在各个工具的配置文件里只写一行引用：

markdown

复制
Include: @./AGENTS.md
一份内容，处处生效，改一处全部同步。

CLAUDE.md 常驻上下文与按需加载规则的分层结构

讲个案例：一条规则从"写了不遵守"到"生效"
光讲原则有点虚，我给你看一个案例，这是我自己项目里真实折腾过一轮的。

我的项目里测试跑一遍全量要四分多钟。Agent 每次改完代码就习惯性地跑 pnpm test，一等就是四分钟，改三轮代码十几分钟就没了。于是我在 CLAUDE.md 里加了一条：

markdown

复制
注意测试的执行效率，避免不必要的全量测试。
你看起来这句话没毛病，其实没用。Agent 照样跑全量测试。当时我的第一反应跟大多数人一样：这模型怎么不听话？是不是降智了？

然后我 Review 了一下这个指令本身，发现了两个问题：

第一，它埋在一个两百多行文件的中部，前后全是同样口吻的"注意"和"避免"，模型很容易忽略掉它；
第二，这句话本身没有可执行性："避免不必要的"——什么算必要？怎么避免？我没说，它只能猜，猜的结果就是不听话。
所以我尝试改成下面这样：

markdown

复制

## 测试

- 跑测试用 `pnpm test <文件路径>`，只跑改动相关的文件
- 全量测试很慢（4 分钟+），避免全量测试，只在我明确要求时才跑
  两个变化：模糊的形容词换成了精确的命令；"不要做什么"配上了"应该做什么"。改完当天就生效了，之后再也没见它主动跑过全量。

这个小案例给我的教训比案例本身值钱：当 Agent 不遵守某条规则时，先别怀疑模型，把那条规则单独拎出来读一遍，按照前文讲的最佳实践分析一遍——你总能找到优化点。

最后：把它当活文档养
还有最后一件事，也是大部分人做得最差的一件事：维护。

很多人用 /init 生成一个 CLAUDE.md，扫一眼觉得"差不多"，就再也没打开过。自动生成的内容恰好是最没价值的部分——它写的都是能从代码里推断出来的东西，而真正值钱的架构决策和业务背景，只有你脑子里有。

Claude Code 的创始人 Boris Cherny 的做法值得学习一下：他自己的 CLAUDE.md 只有 2.5k token（一百多行），提交到 git 里，每次 Claude 犯了一个他没拦住的错，就往里加一条。这个文件在他那里是一个持续进化的错题本，撑起了他同时跑多个 Claude Code 实例的日常工作流。

那错题本模式好在哪里？它天然满足前面讲的所有原则：

每一条都来自真实翻车的经历，所以每一条都是 Agent "猜不到的"；
按需增长，不会一上来就写成百科全书；
而且每条规则都有具体的事故背书，三周后回头看也知道它为什么存在，不容易烂掉。
反过来，定期做减法同样重要。每隔几周让 Agent 自己审一遍这个文件："有没有过时的内容？有没有冗余的规则？"对每一行问一个问题——删掉这行，Agent 会不会犯错？不会就删。这个动作和错题本一增一减，文件才能一直保持在高信噪比的状态。

到这里，我们可以收个尾：CLAUDE.md/AGENTS.md 这个文件的价值不由长度决定，由信噪比决定。目录优于百科全书，精确优于全面，活文档优于一次性工程。

上下文的地基打好了，Agent 每次开工就"懂"你的项目了。但 CLAUDE.md 解决的是跨会话的记忆问题——每次新开会话，Agent 都能从一个正确的起点出发。还有另一半问题我们没讲：会话开着开着，Agent 怎么就"变傻"了？下一章我们来拆这件事——上下文窗口的管理，以及五种让 Agent 保持清醒的操作。
